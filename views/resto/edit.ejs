<div class="col-12 col-lg-6 mx-auto">
  <div class="card">
    <div class="card-body">
      <form action="/resto/<%= resto._id %>/edit" method="post">
          <p><%= resto.name %></p>
          <div class="form-check">
              <input class="form-check-input" type="radio" name="rating" id="flexRadioDefault1" value="3" <%= (review.rating == 3) ? "checked" : ""%>>
              <label class="form-check-label" for="flexRadioDefault1">
                ⭐⭐⭐
              </label>
            </div>
            <div class="form-check">
              <input class="form-check-input" type="radio" name="rating" id="flexRadioDefault2" value="2" <%= (review.rating == 2) ? "checked" : ""%>>
              <label class="form-check-label" for="flexRadioDefault2">
                ⭐⭐
              </label>
            </div>
            <div class="form-check">
              <input class="form-check-input" type="radio" name="rating" id="flexRadioDefault2" value="1" <%= (review.rating == 1) ? "checked" : ""%>>
              <label class="form-check-label" for="flexRadioDefault2">
                ⭐
              </label>
            </div>
            <br>
          <p>Image/Video</p>
          <% if (review != null) {%>
            
          <% } %>
          <div class="mb-3">
              <input type="file" name="reviewFile" class="filepond" id="inputGroupFile01">
          </div>

          <p>Info</p>
          <div class="input-group">
              <textarea class="form-control" name="info"><%= (review != null) ? review.info : "" %></textarea>
          </div>
        <button type="submit" class="btn btn-primary mt-3">Submit</button>
      </form>
      <form action="/resto/<%= resto._id %>/delete" method="post">
        <button type="submit" class="btn btn-danger mt-3">Delete</button>
      </form>
      <a href="/resto/<%= resto._id %>" class="btn btn-primary mt-3">Back</a>
    </div>
  </div>
</div>